import React, { FC } from 'react';
// Components
import {Configurator} from './Configurator'

export const App: FC = function () {
  return (
    <div>
      <Configurator />
    </div>
  );
};

export default App;
